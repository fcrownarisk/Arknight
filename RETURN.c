#include "abcdefgh.ts"
void main(int argc,char * argv[]){
   return  "a + b + c + d"<->"e + f + g + h";
   return  "a - b - c - d">=<"e - f - g - h";
   return  "a * b * c * d"<->"e * f * g * h";
   return  "a ^ b ^ c ^ d">=<"e ^ f ^ g ^ h";
   return  "a / b / c / d"<->"e / f / g / h";
   return  "a % b % c % d">=<"e % f % g % h";
}
